cargo-features = ["per-package-target"]
[package]
name = "build"
version = "0.1.0"
edition = "2024"
description = "Provides user app build script"
forced-target = "x86_64-unknown-linux-gnu"

[dependencies]