//! # 构建环境

/// 此模块用于设置构建环境。
///
/// # 用法
/// ```
/// use build::setup;
/// ```
/// `setup` 函数将初始化构建所需的所有配置和依赖项。
/// # 示例
/// ```
/// setup();
/// ```
/// 确保在调用 `setup` 函数之前，已经正确配置了所有必要的环境变量和依赖项。
use build::setup;

fn main() {
    setup();
}
