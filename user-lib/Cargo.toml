[package]
name = "lib"
version = "0.1.0"
edition = "2024"
description = "Provides access to os"

[lib]
name = "lib"

[dependencies]
common = { path = "../common" }
slab_allocator = { path = "../slab_allocator" }