[package]
name = "slab_allocator"
version = "0.1.0"
edition = "2024"

[lib]
name = "slab_allocator"

[features]
default = ["use_spin"]
use_spin = ["spin"]

[dependencies]
linked_list_allocator = "0.10.5"
spin = { version = "0.9.8", optional = true}