[package]
name = "startup-test"
version = "0.1.0"
edition = "2024"

[dependencies]
sbi-rt = { version = "0.0.3", features = ["legacy"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
