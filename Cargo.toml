[package]
name = "ros666"
version = "0.1.0"
edition = "2024"

[dependencies]
rustsbi = { version = "0.4.0", features = ["forward"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]

members = [
    "user",
    "user-lib",
    "user-build",
]